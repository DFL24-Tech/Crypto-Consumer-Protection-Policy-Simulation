# Copy to .env to override the compose dev defaults.
POSTGRES_USER=dfl24
POSTGRES_PASSWORD=dfl24
POSTGRES_DB=dfl24sim
# Host port Postgres is published on (containers always talk to postgres:5432).
POSTGRES_PORT=5432
